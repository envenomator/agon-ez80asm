; a common operator with different kinds of spacing
; basic spacing before
 ld a,b
  ld a,b
   ld a,b
	ld a,b
		ld a,b
			ld a,b
; spacing immetiately after
	ld a,b
	ld  a,b
	ld   a,b
	ld    a,b
	ld	a,b
	ld		a,b
	ld			a,b